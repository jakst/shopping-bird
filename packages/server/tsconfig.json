{
	"extends": "../../tsconfig.base.json",
	"include": ["./src", "scripts/auth.ts"],
	"files": ["./reset.d.ts"],
	"compilerOptions": {
		"outDir": "dist",

		"types": ["node"],
		"target": "ES2020" /* Set the JavaScript language version for emitted JavaScript and include compatible library declarations. */
	},
	"references": [
		{ "path": "../lib" } //
	]
}
