{
  "name": "shopping-list",
  "private": true,
  "workspaces": {
    "packages": [
      "server",
      "app"
    ]
  },
  "scripts": {
    "app": "yarn workspace happy-bird-app",
    "server": "yarn workspace happy-bird-server"
  },
  "devDependencies": {
    "turbo": "^1.6.3"
  }
}
