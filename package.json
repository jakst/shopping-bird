{
  "name": "hello-bird",
  "private": true,
  "workspaces": {
    "packages": [
      "server",
      "app"
    ]
  },
  "scripts": {
    "app": "yarn workspace hello-bird-app",
    "server": "yarn workspace hello-bird-server"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "rome": "^10.0.1",
    "turbo": "^1.6.3"
  }
}
